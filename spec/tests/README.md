# Specification fixtures

Negative and boundary fixtures for the controlled specification tools live in
the repository `tests/` directory. CI exercises escaped LaTeX separators,
metadata drift, schema rejection, ADR-001 dependency direction, root-concept
ownership, seed namespaces, hazard completeness, and gate blocking.
Repository hygiene, explicit Stage A approval state, and generated-artifact
policy are also enforced by the controlled GitHub Actions workflow.
